# Copy these into Render → Web Service → Environment
# Do NOT commit real secrets to GitHub.

DATABASE_URL=
# ↑ Easiest: Environment → "Add from database" → select your PostgreSQL

JWT_SECRET=
# ↑ Example: logistics_jwt_2026_replace_with_long_random_string

JWT_EXPIRES_IN=8h
CORS_ORIGIN=*
